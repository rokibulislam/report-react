const init = {
    'count': 0
}

const profitReducer = ( state= init, action ) => {
    const newState = { ...state }
    switch( action.type ) {
        default:
            return newState;
    }
}

export default profitReducer;